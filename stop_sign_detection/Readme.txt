*This project is for stop sign detection

*It will recognise the real time objects 

*But it will capture only the stop sign detected frame into the file where the main code is located

*And discard the other frames  

*yolov8n is pre-trained PyTorch weights